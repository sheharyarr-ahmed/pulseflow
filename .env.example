# PulseFlow pipeline secrets — copy to .env locally; add as GitHub Actions repo
# secrets for the cron. Never commit real values.
ANTHROPIC_API_KEY=
SUPABASE_URL=
SUPABASE_SECRET_KEY=
SLACK_WEBHOOK_URL=

# Dashboard env vars (NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY)
# live in Vercel project settings, never in this file or under dashboard/.
