// Publishable-key server client, read-only via RLS.
// Wired at Phase 3 (adds @supabase/supabase-js). No secret key ever lives
// under dashboard/ — writes happen only in the GitHub Actions pipeline.

export {};
